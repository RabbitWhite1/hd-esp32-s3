# AGENTS.md

This file provides guidance to coding agents working in this repository.

## What this is

An Arduino (Arduino-ESP32 framework) firmware for an **ESP32-S3** desktop weather/clock device.
`setup()`/`loop()` live in `hd-esp32-s3.ino`. The device:

- reads temperature/humidity from an **SHTC3** sensor over I2C,
- fetches current+daily weather for a hardcoded list of `cities[]` from `api.open-meteo.com` over HTTPS,
- renders everything to a **300×400 ST7305 reflective monochrome LCD** via U8g2,
- plays a synthesized chime through an **ES8311** codec on boot and on KEY-button press.

There is no build-config file in the repo; the facts below come from reading the source. `README.md` covers
the library/board prerequisites, the build commands, and the Codex token relay.

## Build / flash

This is a plain Arduino sketch — build with `arduino-cli` or the Arduino IDE, not PlatformIO/idf.py.

- **The sketch folder name must match the `.ino` name.** The file on disk is `hd-esp32-s3.ino` inside the `hd-esp32-s3/` folder.
- Target board: ESP32-S3. **PSRAM must be enabled** — the codec echo task allocates with `MALLOC_CAP_SPIRAM`.
- The board has **16 MB flash**, so build with `FlashSize=16M` and a 16 MB partition scheme. Use `PartitionScheme=app3M_fat9M_16MB` (3 MB app partition) — the default scheme only gives ~1.25 MB of app space, which the firmware already nearly fills. With the 3 MB partition the sketch sits around 40%.

Typical commands (adjust the port). Define the FQBN once so compile/upload agree:

```bash
FQBN=esp32:esp32:esp32s3:PSRAM=enabled,FlashSize=16M,PartitionScheme=app3M_fat9M_16MB
arduino-cli compile --fqbn "$FQBN" .
arduino-cli upload  --fqbn "$FQBN" -p /dev/ttyACM0 .
arduino-cli monitor -p /dev/ttyACM0 -c baudrate=115200   # Serial logs are 115200
```

Once a build with `ota.*` in it is running, **`./flash.sh`** does compile + upload in one step, over the air
to `esp32.local` (pass a serial port — `./flash.sh /dev/ttyACM0` — to go over USB instead, which the first
flash still needs).

Required Arduino libraries (installed separately, not vendored): **U8g2**, **ArduinoJson**, **SensorLib** (provides `SensorPCF85063.hpp`). `WiFi`, `WiFiClientSecure`, `HTTPClient`, `SPI` ship with Arduino-ESP32. There is no test suite.

## Architecture

### Project layout

Only `hd-esp32-s3.ino` lives at the sketch root; every other source file is grouped by feature into its own folder under **`src/`** (Arduino compiles the root plus `src/` **recursively**, and silently ignores any other root subfolder — so module folders *must* live under `src/`):

```
hd-esp32-s3.ino
src/
  weather/      weather.{h,cpp}   draw.{h,cpp}      # backend fetch + its LCD rendering
  wifi_net/ time_sync/ sensors/ web_ui/ claude_usage/ codex_usage/ gdoc/ ota/ sdcard/ config/ asset_cache/ history/ logging/   # one folder each
  bsp/          i2c_bsp, i2c_equipment, codec_bsp, ST7305_U8g2
  ExternLib/ Music/   # vendored (see below)
```

Includes use **paths relative to the including file**: the `.ino` includes `"src/<module>/<file>.h"`; a file in `src/foo/` reaches a sibling module with `"../bar/bar.h"` and its own folder with `"file.h"`. (This avoids depending on `-I` flags.) The module table below uses short names — e.g. `weather.*` means `src/weather/weather.{h,cpp}`.

### Frontend / backend split

The sketch (`hd-esp32-s3.ino`) holds **most frontend**: the `drawScreen` compositor, the `draw*` primitives that remain in the sketch, the audio chime + KEY-button handling (user-facing I/O), and `setup()`/`loop()`. It also draws the **doc-update popup** (`drawDocPopup`, overlaid last so the view stays visible around it) whenever `gdocDiffCount() > 0`; while it is up the BOOT button dismisses it (`gdocDiffClear`) instead of cycling the view, and its on-screen `[x]` is drawn at `BTN_BOOT_X` so it lines up under that button. The three `BTN_*_X` constants record where the top-edge buttons sit relative to the header clock (KEY 165, PWR 207, BOOT 257 -- they run KEY, PWR, BOOT left to right). It also owns the shared hardware objects (`I2cbus`, `lcd`, `codec`, and the global `U8G2 *u8g2`). Backend logic lives in dedicated modules and is reached only through their small headers:

| Module | Responsibility | Key API |
|--------|----------------|---------|
| `wifi_net.*` | STA connect/reconnect/status + mDNS (advertises `esp32.local`); holds the mDNS hostname plus a list of saved networks persisted to `/sdcard/wifi.txt` (**no network credentials are baked into the firmware**). `wifiBegin` tries the saved networks and, if every one fails, brings up an open setup AP (`hd-esp32-setup`) with a captive portal so a network can be added from a phone; `wifiAddNetwork` stores a new one **only after it verifies the join** | `wifiBegin`, `wifiEnsureConnected`, `wifiConnected`, `wifiSSID`, `wifiIP`, `wifiHostname`, `wifiLoadNetworks`, `wifiSaveNetworks`, `wifiAddNetwork`, `wifiRemoveNetwork`, `wifiMoveNetwork` / `wifiApplyOrder` / `wifiOrderDirty`, `wifiNetCount` / `wifiNetSSID` |
| `time_sync.*` | NTP sync + dual-TZ formatting. Holds a table of selectable zones (POSIX TZ + "ABBR - City" label); the primary/secondary pick is set via the web UI and persisted in the shared config (`esp32.json`, keys `tz_primary`/`tz_secondary`, written into a freshly created file; defaults: New York / LA) | `timeBegin`, `timeFormatDateTime`, `timeZoneCount` / `timeZoneLabel`, `timePrimaryZone` / `timeSecondaryZone`, `timeSetZones`, `timeLoadZones` / `timeSaveZones` |
| `weather.*` | Open-Meteo fetch (defines `City`, `cities[]`). Cities are added **by name** via the web UI: the name is resolved once to lat/lon through Open-Meteo's **geocoding API** (`geocoding-api.open-meteo.com`), and the resolved coordinates are persisted in the shared config (`esp32.json`, a `cities` array of `{name,lat,lon,label}`). Up to `weatherMaxCities()` (16) cities can be configured, but only the top `weatherShownMax()` (2) are shown on the LCD **and fetched** (`weatherUpdateAll` only refreshes those). The list is priority-ordered like the Wi-Fi list (drag / move-to-top change RAM + mark `weatherOrderDirty`; the web "Save order" persists via `weatherSaveCities`/`weatherApplyOrder`/`weatherMoveCity`). The default seed is just New York, written into a freshly created `esp32.json` (`configWasCreated()`) | `weatherUpdateAll`, `weatherCityCount` / `weatherMaxCities` / `weatherShownMax` / `weatherCityName`, `weatherLoadCities`, `weatherAddCity`, `weatherRemoveCity`, `weatherMoveCity` / `weatherApplyOrder` / `weatherOrderDirty` / `weatherSaveCities` |
| `weather/draw.*` | **Frontend** for weather: renders one city as a gauge + condition icon (uses the global `u8g2`). Lives in the weather folder, called from `drawScreen()` | `drawWeatherRow` |
| `sensors.*` | SHTC3 temp/humidity (owns the `Shtc3Port` instance) | `sensorsBegin(I2cMasterBus&)`, `sensorsPresent`, `sensorsRead` |
| `web_ui.*` | LAN HTTP server (owns a `WebServer` on port 80). Styled with **Bootstrap 5** (loaded from the CDN, with an `asset_cache` SD fallback for offline). Forms submit via **`fetch()`** (no full-page reload): each POST handler ends in `respond(ok,msg)`, which returns JSON `{ok,msg}` to AJAX callers (detected via the `X-Requested-With` header) or falls back to a `flash()` + 303 redirect for no-JS; the client shows a **toast** (green/red) and swaps just the `#app` container with freshly fetched content (preserving the active top-level tab). The NOW card has **two stacked Chart.js line charts** (temperature, then humidity) sharing from/to date pickers + a bucket selector (minutely/hourly/daily/weekly/monthly), fed by **`GET /history?from=&to=&bucket=`** (server-side averaging in `history.*`; auto-refreshes every 60 s). Defaults to today 00:00→tomorrow 00:00 minutely; fixed linear time x-axis (min=from, max=to) so the axes always render even with no data / a future end; ticks are placed at "nice" intervals aligned to local clock boundaries (step scales with span); the humidity y-axis is pinned to 0–100 and the temperature y-axis uses a fixed 10–35 °C indoor band that stretches one-sided by 10% only where data exceeds it. Also serves **`GET /stats`** — a curl-friendly JSON snapshot of date+time, temperature, humidity. Routes: editable to-do checklist shown on the LCD and persisted to `/sdcard/todo.md`, a form that sets the `claude_usage` credentials (two tabs — paste a full browser cookie, or enter org id + key — persisted to `esp32.json` via `claudeUsageSave`), and a Wi-Fi panel that adds a network either by **Add** (`wifiStoreNetwork`, no test) or **Test then Add** (`wifiAddNetwork`, verify-before-save), removes one, and reorders priority by **drag-and-drop** (SortableJS) which only rearranges the list client-side until the user clicks **Save order** (posts the DOM order to `/wifisave` → `wifiApplyOrder` + persist; lights blue while dirty), two time-zone dropdowns (primary/secondary, persisted via `timeSetZones`/`timeSaveZones`), a Weather-cities panel that adds a city by name via `weatherAddCity` (geocoded), removes one, and reorders priority by drag-and-drop / move-to-top + **Save order** (same pattern as Wi-Fi; only the top 2 are shown/fetched), and refresh-interval fields for Claude usage + Codex usage + the Google Doc (minutes, persisted to `esp32.json`). A Codex card takes the ChatGPT access token (pasted at `/codex`, or relayed by cron to the machine-friendly **`/codextoken`**, which answers in plain text rather than the usual toast JSON) and shows the token's decoded expiry. The whole panel is split into **Dashboard** (Now + To-do) and **Configuration** (everything else) top-level tabs, each with its own sticky sub-navigator; a busy overlay spins during each action | `webBegin`, `webHandle`, `webTodoCount` / `webTodoText` / `webTodoDone` |
| `claude_usage.*` | Fetches the claude.ai org usage summary over HTTPS. Org id + `sessionKey` are set at **runtime via the web UI** (no secret in code) and persisted in the shared config (`esp32.json`, keys `claude_org`/`claude_key` — the key sits in plaintext on the SD card, accepted since the device is physically trusted) via `claudeUsageLoad`/`claudeUsageSave`. Endpoint is **authenticated** and may be blocked by Cloudflare/anti-bot from an embedded client. Its auto-refresh interval (minutes, default 30) is web-configurable and persisted in `esp32.json` | `claudeUsageUpdate`, `claudeUsageOk`, `claudeFiveHour`, `claudeSevenDay`, `claudeUsageSetOrgId` / `claudeUsageSetSessionKey`, `claudeUsageIntervalMin` / `claudeUsageSetIntervalMin` |
| `codex_usage.*` | Fetches the ChatGPT/Codex rate-limit snapshot (`GET chatgpt.com/backend-api/wham/usage`, TLS pinned to **GTS Root R4**). Auth is an OAuth access token that only the `codex` CLI can mint and that expires in ~10 days, so it is **relayed in** rather than pasted from a browser: the machine running Codex POSTs `token=` to **`/codextoken`** from an hourly cron (one-liner in the README), and the device fetches its own usage with it. `/codex` is the same thing from the web form. The token is persisted in `esp32.json` (`codex_token`) and its `exp` claim is decoded so the UI can show the expiry and the LCD can say `token expired` instead of surfacing a 401. Windows are reported generically (lengths vary by plan; Plus returns a 7-day primary and no secondary), so the module exposes primary/secondary percent + window length + a short label rather than fixed 5h/7d fields. Refresh interval (minutes, default 30) is web-configurable | `codexUsageUpdate`, `codexUsageOk`, `codexPrimaryPercent` / `codexSecondaryPercent`, `codexPrimaryWindowMin` / `codexSecondaryWindowMin`, `codexPrimaryLabel` / `codexSecondaryLabel`, `codexUsageSetToken` / `codexUsageHasToken` / `codexTokenMatches` / `codexTokenExpiry` / `codexTokenExpired`, `codexUsageLoad` / `codexUsageSave`, `codexUsageIntervalMin` / `codexUsageSetIntervalMin` |
| `gdoc.*` | Fetches a **link-shared Google Doc**'s plain-text export over HTTPS (`export?format=txt`, which 307-redirects to `googleusercontent.com` — redirect-following is on), strips the BOM, and caches ASCII lines (blank lines kept; trailing blanks dropped). The doc title is read from the export's `Content-Disposition` filename. No URL is baked in — it is set entirely at **runtime via the web UI** (a normal Docs link is stored as its base `…/d/<id>` form; the `export?format=txt` suffix is appended only at fetch time) and persisted in the shared config (`esp32.json`, key `gdoc_url`) (empty → `gdocUpdate()` no-ops); the doc must stay shared as "anyone with the link". Its auto-refresh interval (minutes, default 60) is web-configurable and persisted in `esp32.json`. Each fetch is **diffed against the previous revision** (whole-line match, so reordering reports nothing): the added/edited lines of the newest revision accumulate as `<line number> <text>` entries until dismissed (deletions are not reported), and a non-zero `gdocDiffCount()` is what makes the sketch draw the doc-update popup | `gdocUpdate`, `gdocOk`, `gdocLineCount`, `gdocLine`, `gdocTitle`, `gdocAsOf`, `gdocSetUrl` / `gdocUrl` / `gdocLoadUrl` / `gdocSaveUrl`, `gdocIntervalMin` / `gdocSetIntervalMin`, `gdocDiffCount` / `gdocDiffLine` / `gdocDiffTruncated` / `gdocDiffClear` |
| `history.*` | Temperature/humidity logging + range queries. Appends one sample/min to per-year CSVs `/sdcard/sensor_data/sensors_YYYY.csv` (header `timestamp,temperature,humidity`; epoch seconds in UTC, ~12 MB/file/year). `historyQuery(from,to,bucketSec)` reads **only** the requested window — locating the start with a binary search over the file — and averages it into ≤1500 buckets (bucket auto-coarsened if needed; 1500 so a full day fits minutely), returning JSON for the chart, so RAM/transfer stay bounded no matter how big the logs grow | `historyBegin`, `historyAdd`, `historyQuery` |
| `asset_cache.*` | Offline **fallback** cache for the web UI's third-party assets (Bootstrap CSS/JS + SortableJS for drag-reorder + Chart.js for the NOW chart). The page loads each asset from **jsDelivr first** (CSS via `onerror`, JS via a `document.write` fallback if the global is undefined) and only uses the SD copy at `/<asset>` when the CDN is unreachable. The device keeps the SD fallback fresh: downloads are forced uncompressed (`Accept-Encoding: identity`) and content-validated (reject gzip/HTML bodies), re-fetched when missing/corrupt or older than a 1-week TTL; the download loop is bounded (stall/overall timeouts) so a stalled CDN can't hang boot | `assetsEnsureFresh`, `assetCount` / `assetAt` / `assetByRoute`, `assetIsCached` |
| `config.*` | The shared persistent settings store, a single **JSON** file `/sdcard/esp32.json` managed with **ArduinoJson**. Holds the runtime settings that don't warrant their own file: the gdoc + claude-usage refresh intervals, the time-zone selection (`tz_primary`/`tz_secondary`), the Claude credentials (`claude_org`/`claude_key`), the relayed Codex access token (`codex_token`), and the weather-city list (`cities` array). `configBegin()` loads the whole object into an in-RAM `JsonDocument` at boot (a parse error — e.g. a write interrupted by power loss — degrades to an empty store, so all keys fall back to defaults at once); get/set are in-RAM and only hit the card on `configSave()`. Keep each key on one API — ints via `configSetInt`/`configGetInt`, strings via `configSet`/`configGet` (ArduinoJson won't coerce a JSON string back to a number); modules needing structured values (arrays/objects) mutate `configDoc()` directly then `configSave()`. Keys must be string literals (linked, not copied). Feature modules own their key names + defaults, and seed them into a freshly created file when `configWasCreated()` is true (e.g. `timeLoadZones`/`weatherLoadCities` write their defaults through on first boot) | `configBegin`, `configWasCreated`, `configGet` / `configGetInt`, `configSet` / `configSetInt`, `configSave`, `configDoc` |
| `ota.*` | Over-the-air firmware updates via **ArduinoOTA** (espota, UDP/TCP 3232), so a rebuilt sketch can be pushed over the LAN — the `app3M_fat9M_16MB` scheme already has two 3 MB app slots + `otadata`, so the image lands in the idle slot and the next boot switches to it. Optional password from the shared config (`esp32.json`, key `ota_pass`; empty = open, like the web UI). Does **not** re-init mDNS (wifi_net owns the responder) — it only adds the `_arduino._tcp` record. The receive loop blocks inside `otaHandle()`, so the module takes a redraw hook (as `wifi_net` does) and repaints the progress box itself, throttled to every 5% | `otaBegin`, `otaHandle`, `otaSetRedrawHook`, `otaActive` / `otaPercent` / `otaStatus` |
| `sdcard.*` | microSD over **SDMMC** (1-bit) mounted at `/sdcard`; whole-file text read/write + explicit reformat. Mounts with `format_if_mount_failed` so a raw card becomes usable; all ops no-op gracefully when no card is present. Backs the persisted files: `wifi.txt`, `todo.md`, `esp32.json` (the shared config store), the cached web assets under `assets/` (`bootstrap.css` / `bootstrap.js` / `sortable.js` / `chart.js`), and the per-year sensor logs `sensor_data/sensors_YYYY.csv`. `sdPath()` exposes the absolute `/sdcard/<name>` path for callers that stream/stat a file directly (e.g. `asset_cache`, `history`); `sdAppendText()` appends without rewriting. Hot-plug is handled by the sketch: `loop()` polls `sdCardPresent()` (a CMD13) every ~3 s, `sdUnmount()`s a pulled card, and `sdRemount()` + `reloadFromSd()` re-reads all persisted data on re-insert | `sdBegin`, `sdMounted`, `sdCardPresent`, `sdUnmount`, `sdRemount`, `sdFormat`, `sdReadText`, `sdWriteText`, `sdAppendText`, `sdPath` |

The sketch passes `I2cbus` into `sensorsBegin()` and constructs the codec itself, so the shared bus stays owned by the frontend while sensor access is encapsulated. When adding logic, keep this rule: data acquisition / networking / hardware reads go in a backend module; `drawScreen()` (the compositor) stays in the `.ino`. Per-feature **rendering** may live in that feature's folder as a `draw.*` file (as `weather/draw.*` does) and be called from `drawScreen()` — it draws via the shared global `U8G2 *u8g2` (declared `extern` in the draw file, defined in the sketch). Networking that produces non-LCD output still belongs in a backend module — e.g. `web_ui.*` serves an HTML page, but the sketch is what reads the to-do items and renders them.

`logging.*` is a cross-cutting utility used by every module and the sketch: `logDebug/logInfo/logWarn/logError` print to `Serial` with a `yymmdd-hhmmss` local-time prefix and a `[LEVEL]` tag, gated by the compile-time `LOG_LEVEL` constant (Python-`logging` style; defaults to `LOG_DEBUG` = everything). **Prefer these over raw `Serial.print*`** — the only direct `Serial` writes should be `Serial.begin()` and the single sink inside `src/logging/logging.cpp`. Timestamps read ~1970 until `timeBegin()` finishes NTP sync.

### Device drivers (BSP layer)

The backend/frontend code sits on top of hand-written **BSP/port classes** that live together in **`src/bsp/`**; the three I2C devices share a single bus by **reference injection**.

- **`I2cMasterBus` (`i2c_bsp.*`)** — thin wrapper over ESP-IDF `driver/i2c_master.h`. One instance is created in the sketch (`I2cbus(scl=14, sda=13, port 0)`) and passed by `&` into every device class. Convention: a `reg == -1` argument means "no register byte" → raw `i2c_master_transmit`/`receive`; otherwise the register is prepended.
- **`Shtc3Port` (`i2c_equipment.*`)** — SHTC3 temp/humidity driver with CRC checking. Note `SHTC3_PETP_VOL = 4` is **subtracted from the temperature** to compensate for self-heating. The same file also has free functions `Rtc_Setup/SetTime/GetTime` for a **PCF85063 RTC** (via SensorLib, bridged to `I2cMasterBus` through a C callback and file-static singletons). The RTC code is present but not currently called from the sketch.
- **`CodecPort` (`codec_bsp.*`)** — audio over `esp_codec_dev` + `codec_board`. Drives **ES8311** (DAC/speaker, addr 0x18) and **ES7210** (ADC/mic, addr 0x40). The constructor takes a board-profile string (`"S3_RLCD_4_2"`) that selects the I2S/PA pinout baked into `src/ExternLib/codec_board/board_cfg.h`. Provides `CodecPort_CreateMusicTask` (loops the PCM in `src/Music/canon.h`) and `CodecPort_CreateEchoTask` (mic→speaker loopback); the sketch itself uses neither and instead synthesizes a chime inline in `playChime()`.
- **`ST7305_U8g2` (`ST7305_U8g2.*`)** — a custom U8g2 device+byte callback driver for the ST7305 panel, talking SPI directly (not through U8g2's HAL). Key points:
  - Panel is natively **300×400**; the sketch calls `lcd.begin(0, U8G2_R1)`, so logical coordinates are **400×300** (`DISP_W=400`, `DISP_H=300`).
  - `tile_buf_height = 0` selects **full-buffer mode** (~15 KB).
  - U8g2's C callbacks can't capture `this`, so the driver routes through a `static g_lcd_instance` singleton.
  - `U8X8_MSG_DISPLAY_DRAW_TILE` does the non-trivial work: repacking U8g2's 8-px vertical tiles into the ST7305's **3-pixels-per-column-address** layout via the `st_lut` lookup table and column-address arithmetic around base `0x12`.

### Pin map (three separate buses)

| Bus | Pins | Source |
|-----|------|--------|
| Display SPI | sck 11, mosi 12, dc 5, cs 40, rst 41 | `#define`s in the `.ino` |
| I2C (SHTC3 + codec ctrl) | scl 14, sda 13, port 0 | `I2cbus(...)` in the `.ino` |
| Codec I2S + PA | mclk 16, bclk 9, ws 45, din 10, dout 8, PA 46 | `board_cfg.h` (`S3_RLCD_4_2`) |
| KEY button | 18 (active-low, `INPUT_PULLUP`) | `#define` in the `.ino` |
| microSD (SDMMC, 1-bit) | clk 38, cmd 21, d0 39 | `src/sdcard/sdcard.cpp` (from Waveshare `06_SD_Card`) |

Note the I2C pins are declared in **two places** that must agree: the sketch's `I2cbus(14, 13, 0)` and the codec board profile's `i2c: {sda: 13, scl: 14}`.

## `src/ExternLib` — vendored, do not edit

`src/ExternLib/esp_codec_dev` and `src/ExternLib/codec_board` are upstream Espressif ESP-IDF components copied in for the Arduino build (codec drivers for ES8311/ES7210, TCA9554 expander, etc.). Treat them as third-party. The only project-specific file under here is **`board_cfg.h`**, which hardcodes the `S3_RLCD_4_2` profile. `src/Music/canon.h` is a large generated PCM byte array.

## Hardcoded values to be aware of

These are literals, each living with its owning module: the mDNS hostname (`esp32`) in `src/wifi_net/wifi_net.cpp` (no Wi-Fi network is baked into the firmware — networks come only from the SD card / web UI), the default seed city (New York) in `src/weather/weather.cpp` (used only when no cities are configured; written into a freshly created `esp32.json`), the selectable time-zone table (POSIX TZ + label, with the default primary/secondary indices) in `src/time_sync/time_sync.cpp`, the SHTC3 temperature offset in `src/bsp/i2c_equipment.cpp`, **no** claude.ai credentials (both the org id and the `sessionKey` cookie are entered at runtime via the web UI), and the SDMMC pins (clk 38 / cmd 21 / d0 39) in `src/sdcard/sdcard.cpp`. (The Google Doc URL is **not** in code either — set at runtime via the web UI, persisted in `esp32.json`.)

**One-time SD format:** the end of `setup()` calls `sdFormat()` inside a clearly-marked block that **wipes the card on every boot** — it exists only to prepare a raw card and is meant to be commented out once the card is ready (the line after it re-seeds a Wi-Fi network so the wipe doesn't strand the device). Persistent state lives in `/sdcard/esp32.json` (the shared **JSON** settings store, via `config.*`/ArduinoJson — the gdoc + claude + codex refresh intervals, the Google Doc URL, the time-zone selection, the Claude org id + session key, the relayed Codex access token, and the `cities` weather list), `/sdcard/wifi.txt` (one `ssid\tpass` line per saved network, in priority order), `/sdcard/todo.md` (a markdown checklist — `- [x]`/`- [ ]` per item, saved from the web "Save" and loaded in `webBegin()`), the cached web assets under `/sdcard/assets/` (`bootstrap.css` + `bootstrap.js` + `sortable.js` + `chart.js`, refreshed by `asset_cache` with a 1-week TTL), and the per-year sensor logs `/sdcard/sensor_data/sensors_YYYY.csv` (one sample/min from `history.*`). `configBegin()` runs first in `setup()`, then the modules that read from it (`claudeUsageLoad`, `codexUsageLoad`, `timeLoadZones`, `weatherLoadCities`) plus `wifiLoadNetworks`/`gdocLoadUrl`, all before `wifiBegin()`.
